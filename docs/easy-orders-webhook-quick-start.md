# دليل سريع: إعداد Webhook EasyOrders

دليل سريع خطوة بخطوة لإعداد Webhook EasyOrders وضمان وصول الطلبات.

---

## 🚀 الإعداد السريع (5 دقائق)

### الخطوة 1: إنشاء التكامل
1. اذهب إلى `/dashboard/integrations`
2. اضغط **"إضافة تكامل"**
3. اختر **"الربط التلقائي"** (موصى به)
4. اتبع التعليمات في EasyOrders

### الخطوة 2: التحقق من Webhook URL
1. بعد إنشاء التكامل، اضغط **"عرض معلومات Webhook"**
2. انسخ **Webhook URL**
3. تأكد من أن URL **عام** (ليس localhost)

### الخطوة 3: إعداد Webhook في EasyOrders
1. اذهب إلى **EasyOrders Dashboard > Public API > Webhooks**
2. أضف **Webhook URL** الذي نسخته
3. انسخ **Webhook Secret** من EasyOrders

### الخطوة 4: حفظ Webhook Secret
1. في ربح، اذهب إلى إعدادات التكامل
2. أضف **Webhook Secret** (إذا لم يتم حفظه تلقائياً)
3. احفظ التغييرات

### الخطوة 5: التحقق والاختبار
1. اضغط **"التحقق من Webhook"** في صفحة التكاملات
2. إذا كانت هناك مشاكل، اتبع التوصيات
3. اضغط **"اختبار Webhook"** لإرسال طلب تجريبي

---

## ✅ التحقق السريع

### من الواجهة:
1. **التحقق من Webhook:**
   - اضغط "التحقق من Webhook" على التكامل
   - تحقق من النتيجة:
     - ✓ **صحيحة** → Webhook مضبوط بشكل صحيح
     - ✗ **مشاكل** → اتبع التوصيات

2. **اختبار Webhook:**
   - اضغط "اختبار Webhook"
   - تحقق من النتيجة:
     - ✓ **نجح** → Webhook يعمل
     - ✗ **فشل** → راجع الخطوات أعلاه

3. **عرض معلومات Webhook:**
   - اضغط "عرض معلومات Webhook"
   - تحقق من:
     - Webhook URL
     - Webhook Secret (محفوظ ✓)
     - Store ID
     - إجمالي الطلبات

---

## 🔍 المشاكل الشائعة

### المشكلة: Webhook URL يستخدم localhost
**الحل:**
- استخدم **ngrok** للتطوير المحلي:
  ```bash
  ngrok http 3000
  ```
- أو استخدم **Vercel** للإنتاج

### المشكلة: Webhook Secret غير محفوظ
**الحل:**
1. انسخ Webhook Secret من EasyOrders
2. أضفه في إعدادات التكامل في ربح
3. أو انتظر حتى يتم حفظه تلقائياً

### المشكلة: لا توجد طلبات
**الحل:**
1. تحقق من أن Webhook مضبوط في EasyOrders
2. أنشئ طلب تجريبي في EasyOrders
3. تحقق من Logs في ربح

---

## 📊 فحص الحالة

### من API:
```bash
# التحقق من حالة Webhook
POST /api/integrations/easy-orders/webhook/verify
{
  "integrationId": "YOUR_INTEGRATION_ID"
}

# اختبار Webhook
POST /api/integrations/easy-orders/webhook/test
{
  "integrationId": "YOUR_INTEGRATION_ID"
}

# عرض معلومات Webhook
GET /api/integrations/easy-orders/webhook/status
```

---

## ✅ قائمة التحقق النهائية

- [ ] التكامل تم إنشاؤه
- [ ] Webhook URL عام (ليس localhost)
- [ ] Webhook URL مضبوط في EasyOrders
- [ ] Webhook Secret محفوظ
- [ ] التحقق من Webhook نجح
- [ ] اختبار Webhook نجح
- [ ] الطلبات تصل (فحص Logs)

---

## 🆘 الحصول على المساعدة

إذا واجهت مشاكل:
1. اتبع **دليل التشخيص الكامل**: `docs/easy-orders-webhook-troubleshooting-checklist.md`
2. فحص **Server Logs** للبحث عن أخطاء
3. تحقق من **Webhook Logs** في EasyOrders Dashboard

---

**آخر تحديث:** 2024

