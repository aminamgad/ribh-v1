# دليل المسوق: ربط Easy Orders وتصدير المنتجات

هذا الدليل يوضح للمسوق كيفية ربط متجره مع Easy Orders وتصدير منتجاته.

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من:
- ✅ لديك حساب Easy Orders نشط
- ✅ لديك API Key من Easy Orders مع الصلاحيات التالية:
  - `products:read`
  - `products:create`
  - `products:update`
  - `orders:read`
  - `shipping_areas`
- ✅ لديك منتجات معتمدة في نظام ربح

---

## 🔗 الخطوة 1: ربط Easy Orders

### الربط التلقائي

الربط مع Easy Orders يتم تلقائياً في خطوة واحدة:

1. **انتقل إلى صفحة التكاملات:**
   - من القائمة الجانبية، اضغط على "التكاملات" أو انتقل مباشرة إلى `/dashboard/integrations`

2. **إضافة تكامل جديد:**
   - اضغط على زر "إضافة تكامل" في أعلى الصفحة

3. **اختر Easy Orders:**
   - في النافذة المنبثقة، اختر "EasyOrders" من خيارات المنصة

4. **الربط التلقائي:**
   - ستجد قسم "الربط التلقائي"
   - اضغط على زر "ربط تلقائي مع EasyOrders"
   - سيتم فتح نافذة جديدة مع رابط التثبيت

5. **الموافقة في Easy Orders:**
   - في نافذة Easy Orders، قم بالموافقة على التثبيت
   - سيتم إنشاء API Key و Webhook تلقائياً

6. **اكتمال الربط:**
   - سيتم إعادة التوجيه تلقائياً إلى صفحة التكاملات
   - ستظهر رسالة نجاح: "تم ربط EasyOrders بنجاح!"
   - ستجد تكامل جديد بحالة "نشط" مع Store ID

> **ملاحظة:** الربط التلقائي يتطلب URL عام (Vercel أو ngrok). للتطوير المحلي، استخدم ngrok للحصول على URL عام.

---

## 🚚 الخطوة 2: مزامنة الشحن (اختياري)

بعد ربط Easy Orders، يمكنك مزامنة مناطق الشحن:

1. **من صفحة التكاملات:**
   - ابحث عن التكامل الذي أنشأته
   - اضغط على زر "مزامنة الشحن"

2. **انتظار اكتمال العملية:**
   - ستظهر رسالة نجاح: "تمت مزامنة X مدينة بنجاح"
   - سيتم تحديث `lastShippingSync` في التكامل

3. **التحقق:**
   - انتقل إلى Easy Orders Dashboard
   - تحقق من إعدادات الشحن
   - يجب أن تظهر المدن والتكاليف المحددة

---

## 📦 الخطوة 3: تصدير المنتجات

بعد ربط Easy Orders بنجاح، يمكنك تصدير منتجاتك. حالياً، التصدير متاح عبر API فقط. سنضيف واجهة مستخدم قريباً.

### تصدير منتج واحد

استخدم API endpoint التالي:

```bash
POST /api/integrations/easy-orders/export-product
Content-Type: application/json

{
  "productId": "PRODUCT_ID",
  "integrationId": "INTEGRATION_ID"
}
```

**مثال باستخدام JavaScript/Fetch:**

```javascript
const exportProduct = async (productId, integrationId) => {
  try {
    const response = await fetch('/api/integrations/easy-orders/export-product', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        productId: productId,
        integrationId: integrationId
      })
    });

    const data = await response.json();
    
    if (response.ok) {
      console.log('تم تصدير المنتج بنجاح!', data);
      // رسالة نجاح
    } else {
      console.error('فشل التصدير:', data.error);
      // رسالة خطأ
    }
  } catch (error) {
    console.error('حدث خطأ:', error);
  }
};
```

**النتيجة المتوقعة:**
- ✅ رسالة نجاح
- ✅ حفظ `easyOrdersProductId` في metadata المنتج
- ✅ ظهور المنتج في Easy Orders Dashboard

### تصدير عدة منتجات (Bulk Export)

لتصدير عدة منتجات دفعة واحدة:

```bash
POST /api/integrations/easy-orders/export-bulk
Content-Type: application/json

{
  "productIds": ["ID1", "ID2", "ID3"],
  "integrationId": "INTEGRATION_ID"
}
```

**مثال باستخدام JavaScript/Fetch:**

```javascript
const exportBulkProducts = async (productIds, integrationId) => {
  try {
    const response = await fetch('/api/integrations/easy-orders/export-bulk', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        productIds: productIds,
        integrationId: integrationId
      })
    });

    const data = await response.json();
    
    if (response.ok) {
      console.log(`تم تصدير ${data.results.success.length} منتج بنجاح`);
      console.log('المنتجات الناجحة:', data.results.success);
      console.log('المنتجات الفاشلة:', data.results.failed);
    } else {
      console.error('فشل التصدير:', data.error);
    }
  } catch (error) {
    console.error('حدث خطأ:', error);
  }
};
```

**ملاحظات مهمة:**
- ⏱️ يتم احترام Rate Limit (1.5 ثانية بين كل طلب)
- ✅ يتم إرجاع قائمة بالمنتجات الناجحة والفاشلة
- 🔄 إذا كان المنتج موجوداً بالفعل في Easy Orders، سيتم تحديثه

---

## 🛒 الخطوة 4: استقبال الطلبات (Webhook)

بعد تصدير المنتجات، يمكنك استقبال الطلبات من Easy Orders تلقائياً:

### إعداد Webhook في Easy Orders:

1. **انتقل إلى Easy Orders Dashboard:**
   - اذهب إلى Public API > Webhooks

2. **أضف Webhook URL:**
   ```
   https://your-domain.com/api/integrations/easy-orders/webhook
   ```

3. **انسخ Webhook Secret:**
   - احفظ Webhook Secret في مكان آمن

### تحديث التكامل:

1. **من صفحة التكاملات:**
   - اضغط على "الإعدادات" للتكامل
   - أضف Webhook Secret (إذا لم يتم إضافته تلقائياً)
   - احفظ التغييرات

### التحقق:

1. **أنشئ طلب تجريبي في Easy Orders**
2. **تحقق من استقبال الطلب:**
   - انتقل إلى `/dashboard/orders`
   - يجب أن يظهر طلب جديد مع Badge "EasyOrders"
   - عرض Easy Orders Order ID في تفاصيل الطلب

---

## 🔍 الخطوة 5: فلترة الطلبات

يمكنك فلترة الطلبات حسب المصدر:

1. **انتقل إلى `/dashboard/orders`**
2. **في الفلاتر:**
   - اختر "مصدر الطلب"
   - اختر "EasyOrders" أو "ربح فقط"
3. **النتيجة:**
   - عرض الطلبات المفلترة فقط
   - Badge "EasyOrders" على الطلبات القادمة من Easy Orders

---

## 🔄 تحديث المنتجات

إذا قمت بتحديث منتج في نظام ربح (السعر، الكمية، إلخ):

1. **قم بتصدير المنتج مرة أخرى:**
   - استخدم نفس API endpoint لتصدير المنتج
   - سيتم تحديث المنتج في Easy Orders تلقائياً

2. **النتيجة:**
   - ✅ تحديث المنتج في Easy Orders
   - ✅ رسالة: "تم تحديث المنتج بنجاح"

---

## ⚠️ استكشاف الأخطاء

### المشكلة: فشل الربط التلقائي
**الحل:**
- تحقق من Base URL في `.env`
- تحقق من Callback URL
- تحقق من الصلاحيات المطلوبة

### المشكلة: فشل مزامنة الشحن
**الحل:**
- تحقق من وجود مناطق شحن في النظام
- تحقق من API Key permissions
- تحقق من تنسيق البيانات (city:cost)

### المشكلة: فشل استقبال الطلبات
**الحل:**
- تحقق من Webhook Secret
- تحقق من Webhook URL
- استخدم webhook.site للاختبار

### المشكلة: Rate Limit Exceeded
**الحل:**
- انتظر دقيقة واحدة
- قلل عدد المنتجات في Bulk Export
- استخدم Caching

### المشكلة: المنتج غير معتمد
**الحل:**
- تأكد من أن المنتج معتمد (`isApproved: true`)
- تأكد من أن المنتج نشط (`isActive: true`)
- انتظر موافقة الإدارة على المنتج

---

## 📊 ملاحظات مهمة

### Status Mapping

عند استقبال طلب من Easy Orders، يتم تحويل الحالات بشكل تلقائي:

| Easy Orders Status | Ribh Status |
|-------------------|-------------|
| pending | pending |
| confirmed | confirmed |
| paid | confirmed |
| processing | processing |
| waiting_for_pickup | ready_for_shipping |
| in_delivery | out_for_delivery |
| delivered | delivered |
| canceled | cancelled |
| refunded | refunded |

**ملاحظة مهمة:**
- ❌ **لا يتم تحديث حالة Easy Orders من نظام ربح**
- ✅ يتم فقط استقبال الطلبات وعرضها

---

## ✅ قائمة التحقق

استخدم هذه القائمة للتأكد من إكمال جميع الخطوات:

- [ ] ربط Easy Orders (تلقائي)
- [ ] التحقق من حالة التكامل (نشط)
- [ ] مزامنة الشحن (اختياري)
- [ ] تصدير منتج واحد للاختبار
- [ ] التحقق من ظهور المنتج في Easy Orders
- [ ] إعداد Webhook (إذا لزم الأمر)
- [ ] اختبار استقبال طلب من Easy Orders
- [ ] تصدير باقي المنتجات

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. تحقق من السجلات (Logs)
2. تحقق من حالة التكامل
3. راجع رسائل الخطأ
4. تواصل مع فريق الدعم

---

**آخر تحديث:** 2024


