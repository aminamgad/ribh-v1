# قائمة مهام وتحسينات UI/UX - منصة ربح

تم إعداد هذه القائمة بعد فحص جميع صفحات ومكونات الموقع.

---

## 🔴 مشاكل حرجة (يجب إصلاحها)

### 1. `animate-float` غير معرّف
- **الموقع:** صفحة تسجيل الدخول و التسجيل
- **المشكلة:** الكلاس `animate-float` مستخدم في عناصر الخلفية الزخرفية لكنه غير معرّف في `globals.css`
- **الحل:** إضافة `@keyframes float` وكلاس `.animate-float` في globals.css

### 2. رابط "تعرف على المزيد" لا يعمل
- **الموقع:** الصفحة الرئيسية - قسم Hero
- **المشكلة:** الزر يوجه إلى `#features` لكن لا يوجد عنصر بهذا الـ id
- **الحل:** إضافة `id="features"` لقسم "Value Proposition" أو القسم المناسب

### 3. صفحة نسيت كلمة المرور غير موجودة
- **الموقع:** صفحة تسجيل الدخول
- **المشكلة:** رابط "نسيت كلمة المرور؟" يشير إلى `/auth/forgot-password` ولا توجد هذه الصفحة
- **الحل:** إنشاء الصفحة أو إخفاء الرابط مؤقتاً

### 4. Toast CSS يستهدف مكتبة خاطئة
- **الموقع:** `globals.css` - قسم Mobile Toast Notifications
- **المشكلة:** CSS يستهدف `[data-sonner-toast]` بينما التطبيق يستخدم `react-hot-toast`
- **الحل:** تحديث الـ selectors لتناسب react-hot-toast أو إزالة قواعد sonner

---

## 🟠 مشاكل متوسطة (تحسينات مهمة)

### 5. أيقونات مكررة في BottomNav لتاجر الجملة
- **الموقع:** `BottomNavBar.tsx` - حالة wholesaler
- **المشكلة:** الطلبات والسلة يستخدمان نفس الأيقونة (ShoppingBag) مما يسبب التباساً
- **الحل:** استخدام `ShoppingCart` للسلة بدلاً من ShoppingBag للطلبات

### 6. النسب المئوية الثابتة في لوحة التحكم
- **الموقع:** `dashboard/page.tsx` - بطاقات الإحصائيات
- **المشكلة:** النسب (+12.5%, +8.3%, +15.2%...) مكتوبة يدوياً وليست من البيانات الفعلية
- **الحل:** استخدام `stats?.ordersPercentage` و `stats?.revenuePercentage` وغيرها من الـ API

### 7. Padding زائد في عناصر الطلبات الأخيرة
- **الموقع:** لوحة التحكم - قسم "آخر الطلبات"
- **المشكلة:** عناصر `<p>` تحتوي على `p-4` مما يسبب مسافات غير منطقية
- **الحل:** إزالة الـ padding الزائد من النصوص الداخلية

### 8. `gradient-text` بألوان غير متناسقة
- **الموقع:** لوحة التحكم - عنوان "لوحة تحكم المورد/المسوق..."
- **المشكلة:** الكلاس `gradient-text` في globals.css يستخدم ألوان أزرق/بنفسجي بدلاً من برتقالي العلامة التجارية
- **الحل:** تعديل gradient-text ليستخدم from-[#FF9800] to-[#F57C00]

### 9. روابط الفوتر غير مفعّلة
- **الموقع:** الصفحة الرئيسية - Footer
- **المشكلة:** روابط "الأسئلة الشائعة"، "الدعم الفني"، "سياسة الخصوصية"، "الشروط والأحكام" نصوص فقط وليست روابط
- **الحل:** إنشاء الصفحات أو ربطها بـ # مؤقتاً

### 10. سنة الفوتر ثابتة
- **الموقع:** الصفحة الرئيسية - Footer
- **المشكلة:** يظهر "© 2024" بشكل ثابت
- **الحل:** استخدام `{new Date().getFullYear()}` لعرض السنة الحالية

### 11. تكرار حقل البحث عن المنتجات ✅ (الحل معرّف)
- **الموقع:** `DashboardHeader` + صفحة المنتجات (`SearchFilters`)
- **المشكلة:** حقل بحث المنتجات يظهر في مكانين. حالة غير متزامنة. سياق خاطئ في صفحات الطلبات والمفضلة.
- **الحل المُوصى به:** العرض الشرطي — إظهار البحث في الـ Header فقط خارج صفحة المنتجات، وفي SearchFilters داخل صفحة المنتجات. انظر القسم "حقل البحث والفلاتر المتكررة" للتفاصيل وخطوات التنفيذ.

---

## 🟡 تحسينات UX عامة

### 12. توحيد Focus Ring
- **المشكلة:** بعض الـ inputs تستخدم `focus:ring-blue-500` بدلاً من ألوان العلامة التجارية
- **الحل:** مراجعة جميع العناصر التفاعلية واستخدام `focus:ring-[#FF9800]` أو متغيرات الـ theme

### 13. تجربة الفلاتر على الموبايل
- **الموقع:** SearchFilters و OrdersFilters
- **المشكلة:** الفلاتر قد تكون معقدة أو صغيرة على الشاشات الصغيرة
- **الحل:** تحسين عرض الفلاتر كـ drawer أو accordion على الموبايل، وزيادة مناطق اللمس

### 14. إدارة التركيز في النوافذ المنبثقة
- **الموقع:** ConfirmationModal وجميع الـ modals
- **المشكلة:** قد لا يلتقط focus داخل الـ modal ولا يمنع Tab من الخروج
- **الحل:** إضافة focus trap و return focus عند الإغلاق (aria-modal, tabIndex)

### 15. توحيد الحالات الفارغة والتحميل
- **المشكلة:** صفحات مختلفة تستخدم تصاميم مختلفة للـ loading و empty states
- **الحل:** إنشاء مكونات موحدة: `EmptyState`, `LoadingState` أو `SkeletonCard` قابلة لإعادة الاستخدام

### 16. زر "تذكرني" غير فعّال
- **الموقع:** صفحة تسجيل الدخول
- **المشكلة:** الـ checkbox موجود لكن لا يوجد منطق لحفظ حالة تسجيل الدخول
- **الحل:** تنفيذ Remember Me عبر localStorage/cookies أو إزالة الخيار

### 17. ErrorBoundary يستخدم ألوان أزرق بدلاً من العلامة التجارية
- **الموقع:** `components/ErrorBoundary.tsx`
- **المشكلة:** زر "إعادة المحاولة" يستخدم `bg-blue-600` بدلاً من برتقالي العلامة التجارية
- **الحل:** استبدال `bg-blue-600 hover:bg-blue-700` بـ `btn-primary` أو ألوان #FF9800

### 18. تكرار غير متناسق في ألوان الـ Modals
- **الموقع:** TemplateNameModal, DraftRestoreModal, TemplateLoadConfirmModal
- **المشكلة:** أزرار التأكيد تستخدم `from-purple-600 to-indigo-600` أو `from-blue-600 to-indigo-600` بدلاً من ألوان العلامة التجارية
- **الحل:** توحيد أزرار التأكيد مع `btn-primary` أو ألوان #FF9800

### 19. عدم اتساق مؤشرات التحميل (Loading)
- **الموقع:** صفحات متعددة
- **المشكلة:** بعض الصفحات تستخدم `border-primary-600`، بعضها `border-blue-600`، وبعضها `loading-spinner`. صفحة المنتجات لديها skeleton كامل بينما الأخرى تستخدم spinner بسيط.
- **الحل:** توحيد مؤشر التحميل عبر مكوّن `LoadingSpinner` أو `Skeleton` مشترك

---

## 🟢 تحسينات مقترحة إضافية

### 20. Breadcrumbs
- إضافة مسار تنقل (breadcrumbs) في صفحات لوحة التحكم للمساعدة في التنقل

### 21. دعم تقليل الحركة
- الـ globals.css يحتوي على `prefers-reduced-motion` - التأكد من تطبيقه بشكل كامل على جميع الحركات

### 22. تحسين تجربة سلة التسوق
- صفحة السلة طويلة؛ يمكن تقسيمها إلى خطوات (عناوين، ملخص، دفع) لتحسين UX

### 23. تحسين جداول الموبايل
- جداول الطلبات والمنتجات قد تحتاج عرض بطاقات (card view) على الموبايل بدلاً من الجدول الأفقي

### 24. توحيد تصميم الـ Modals
- يوجد أكثر من نمط للـ modals (inline styles, ConfirmationModal) - توحيد التصميم

### 25. عناوين الصفحات الديناميكية
- استخدام `document.title` أو metadata في Next.js لعناوين صفحات مناسبة لكل صفحة

### 26. رسائل خطأ أفضل
- تحسين عرض رسائل الخطأ بتصميم موحد وواضح

### 27. تحسين الـ Mobile Drawer
- `mobile-drawer-content` يستخدم `translate-x` - التحقق من سلوك RTL الصحيح

### 28. تكرار تعريف الـ CSS
- `.card` و `.btn-primary` معرّفة مرتين (في base و components layers) - يمكن دمجها

### 29. Marketer BottomNav بدون السلة
- المسوق يحتاج الوصول للسلة لكن BottomNav قد لا يعرضها - التحقق من العناصر المعروضة

### 30. ConfirmationModal بدون focus trap
- **الموقع:** `components/ui/ConfirmationModal.tsx`
- **المشكلة:** لا يحتوي على aria-modal أو focus trap؛ المستخدم يمكنه الضغط Tab والخروج من الـ modal
- **الحل:** إضافة `aria-modal="true"` و focus trap (useEffect لالتقاط التركيز ومنع Tab من الخروج)

### 31. استخدام واسع لألوان blue للمعلومات
- **الموقع:** صفحات الطلبات، الشحن، القرية، الإعدادات
- **المشكلة:** استخدام `text-blue-600`, `bg-blue-50`, `border-blue-200` للمعلومات والشحن — قد يخلط المستخدم بين blue والعلامة التجارية (برتقالي/أخضر)
- **الحل:** تقييد استخدام blue لعناصر "معلوماتية" فقط، أو استبدالها بـ slate/gray للتمييز عن أزرار الإجراءات

### 32. خطأ محتمل في Favorites page
- **الموقع:** `app/dashboard/favorites/page.tsx` — interface Product
- **المشكلة:** الحقل `wholesalePrice` قد يكون خطأً؛ المنتجات في النظام تستخدم `wholesalerPrice`
- **الحل:** التحقق والتصحيح إلى `wholesalerPrice`

---

## 📋 حقل البحث والفلاتر المتكررة — تحليل وحلول

### الوضع الحالي

| الموقع | المكون | الوظيفة | مصدر الحالة |
|--------|--------|---------|-------------|
| **Header** (ديسكتوب) | `DashboardHeader` | بحث منتجات → ينتقل إلى `/products?q=...` | state محلي |
| **صفحة المنتجات** | `SearchFilters` | بحث + فلاتر (فئة، سعر، ترتيب...) | URL + sessionStorage |

### المشاكل

1. **تكرار**: حقل بحث المنتجات في مكانين (Header + SearchFilters)
2. **عدم مزامنة الحالة**: عند فتح `/products?q=iphone` حقل الـ Header يظهر فارغاً
3. **سياق خاطئ**: الـ Header يعرض دائماً "البحث عن المنتجات" حتى في صفحات الطلبات والمفضلة
4. **موبايل**: البحث في الـ Header مخفي، فقط SearchFilters يظهر

### الحلول المقترحة

#### الحل 1: مصدر واحد للحقيقة (URL) — مُوصى به

**الفكرة:** الـ URL هو المصدر الوحيد؛ Header وصفحة المنتجات يقرآن ويكتبان منه.

**التنفيذ:**
- Header يقرأ `q` من URL عندما `pathname === /dashboard/products`
- عند الكتابة/الإرسال في Header → تحديث URL
- SearchFilters يقرأ ويكتب نفس الـ URL
- حقل بحث واحد فقط (إما Header أو SearchFilters، ليس الاثنين)

**النتيجة:** لا تكرار، حالة متزامنة، روابط قابلة للمشاركة.

#### الحل 2: Header كـ Quick Search فقط

**الفكرة:** Header للانتقال السريع فقط، لا مزامنة.

**التنفيذ:**
- Header ينتقل إلى `/products?q=...` عند الإرسال
- عند الدخول لصفحة المنتجات، الـ Header يهيّئ قيمته من `q` في URL (قراءة فقط بعد التنقل)

**النتيجة:** تحسين بسيط مع تعديلات قليلة.

#### الحل 3: إخفاء البحث من Header على صفحة المنتجات

**الفكرة:** عدم تكرار البحث في نفس الصفحة.

**التنفيذ:**
- إظهار حقل البحث في Header فقط عندما `pathname !== /dashboard/products`
- في صفحة المنتجات: الاعتماد كلياً على SearchFilters

**النتيجة:** حل سريع، لكن المستخدم يفقد البحث من الـ Header وهو على المنتجات.

#### الحل 4: Context للبحث

**الفكرة:** مشاركة حالة البحث عبر React Context.

**التنفيذ:**
- إنشاء `SearchContext` يحمل `productSearchQuery` و `setProductSearchQuery`
- Header وصفحة المنتجات يستهلكان نفس الـ Context
- التحديثات تكتب في الـ URL أيضاً للمشاركة

**النتيجة:** حالة موحدة، يتطلب إدارة جيدة لربط الـ URL بالـ Context.

### مقارنة الحلول

| الحل | التعقيد | تجربة المستخدم | روابط قابلة للمشاركة |
|------|---------|----------------|------------------------|
| 1. مصدر واحد (URL) | متوسط | ممتازة | ✅ |
| 2. Quick Search | منخفض | جيدة | ✅ |
| 3. إخفاء على المنتجات | منخفض جداً | مقبولة | ✅ |
| 4. Context | أعلى | ممتازة | ✅ (مع ربط URL) |

### الحل المُوصى به النهائي ✅

**دمج الحل 1 + الحل 3: العرض الشرطي + URL كمصدر وحيد**

| الموقع | حقل البحث | السلوك |
|--------|-----------|--------|
| خارج صفحة المنتجات | في الـ Header | بحث سريع → ينتقل إلى `/products?q=...` |
| في صفحة المنتجات | في SearchFilters فقط | البحث + الفلاتر، الـ URL هو المصدر الوحيد |

**النتيجة:** حقل بحث واحد مرئي دائماً، لا تكرار، الموبايل يُغطى عبر SearchFilters، الـ URL صالح للمشاركة.

---

### خطوات التنفيذ التفصيلية

#### 1. تعديل `DashboardHeader.tsx`

- استيراد `usePathname` و `useSearchParams` من `next/navigation`
- إظهار حقل البحث **فقط** عندما `!pathname.startsWith('/dashboard/products')`
- عند الإرسال: `router.push(/dashboard/products?q=...)` أو `router.replace` حسب الحاجة
- مزامنة قيمة الـ input مع `q` من الـ URL عند الدخول من رابط (اختياري لتحسين UX)

```tsx
const pathname = usePathname();
const isOnProductsPage = pathname.startsWith('/dashboard/products');

// إظهار البحث فقط خارج صفحة المنتجات
{!isOnProductsPage && (
  <div className="hidden md:flex flex-1 max-w-md mx-4 lg:mx-8">
    <form onSubmit={handleSearch}>...</form>
  </div>
)}
```

#### 2. التحقق من `SearchFilters.tsx`

- التأكد أنه يقرأ ويكتب `q` في الـ URL (مع `window.location.search` أو `useSearchParams`)
- لا تعديل مطلوب إذا كان يعتمد على الـ URL بالفعل

#### 3. الملفات المتأثرة

| الملف | التعديل |
|-------|---------|
| `components/dashboard/DashboardHeader.tsx` | إضافة شرط `!isOnProductsPage` حول حقل البحث |
| `components/search/SearchFilters.tsx` | التحقق فقط — لا تغيير إذا كان متوافقاً مع الـ URL |

---

### للمشاكل المشابهة (فلاتر، بحث الطلبات...)

- استخدام **URL كمصدر وحيد** لجميع الفلاتر والبحث
- إنشاء **Context موحد للفلاتر** يقرأ ويكتب في الـ URL
- توحيد **مكونات مشتركة** (`SearchInput`, `FilterPanel`, `DateRangePicker`) وإعادة استخدامها في المنتجات والطلبات

---

## ملخص الأولويات

| الأولوية | العدد | الحالة |
|----------|-------|--------|
| 🔴 حرجة | 4 | تحتاج إصلاح فوري |
| 🟠 متوسطة | 7 | مهمة خلال أسبوع |
| 🟡 تحسينات | 9 | خلال Sprint |
| 🟢 مقترحة | 14 | Backlog |

---

*تم إنشاء هذه القائمة بتاريخ: فبراير 2025*  
*آخر تحديث: فحص إضافي — إضافة 8 مشاكل وتحسينات جديدة (ErrorBoundary، Modals، Loading، Focus trap، ألوان blue، Favorites)*
