# دليل اختبار تكامل EasyOrders

هذا الدليل يوضح كيفية اختبار جميع ميزات التكامل مع EasyOrders.

## 📋 المتطلبات الأساسية

1. حساب EasyOrders نشط
2. URL عام (Vercel أو ngrok) للربط التلقائي
3. حساب مسوق في نظام ربح

> **ملاحظة:** الربط مع EasyOrders يتم تلقائياً فقط. لا يمكن إدخال API Key يدوياً.

---

## 🔗 1. اختبار الربط التلقائي

### الخطوات:
1. انتقل إلى `/dashboard/integrations`
2. اضغط على "إضافة تكامل"
3. اختر "EasyOrders"
4. اضغط على "ربط تلقائي مع EasyOrders"
5. سيتم فتح نافذة جديدة مع رابط التثبيت
6. قم بالموافقة على التثبيت في EasyOrders
7. سيتم إعادة التوجيه تلقائياً إلى صفحة التكاملات

### النتيجة المتوقعة:
- ✅ ظهور تكامل جديد بحالة "نشط"
- ✅ عرض Store ID
- ✅ حفظ API Key و Webhook Secret تلقائياً

---

## 🚚 2. اختبار مزامنة الشحن

### الخطوات:
1. تأكد من وجود مناطق شحن في نظام ربح (من إعدادات الإدارة)
2. انتقل إلى `/dashboard/integrations`
3. اضغط على "مزامنة الشحن" للتكامل
4. انتظر اكتمال العملية

### النتيجة المتوقعة:
- ✅ رسالة نجاح: "تمت مزامنة X مدينة بنجاح"
- ✅ تحديث `lastShippingSync` في التكامل
- ✅ `shippingSynced` = true

### التحقق:
- انتقل إلى EasyOrders Dashboard
- تحقق من إعدادات الشحن
- يجب أن تظهر المدن والتكاليف المحددة

---

## 📦 3. اختبار تصدير منتج واحد

### الخطوات:
1. تأكد من وجود منتج معتمد في نظام ربح
2. استخدم API endpoint:
   ```bash
   POST /api/integrations/easy-orders/export-product
   {
     "productId": "PRODUCT_ID",
     "integrationId": "INTEGRATION_ID"
   }
   ```
3. أو أضف زر تصدير في واجهة المنتجات (اختياري)

### النتيجة المتوقعة:
- ✅ رسالة نجاح
- ✅ حفظ `easyOrdersProductId` في metadata المنتج
- ✅ ظهور المنتج في EasyOrders Dashboard

### التحقق:
- انتقل إلى EasyOrders Dashboard
- تحقق من وجود المنتج
- تحقق من جميع البيانات (السعر، الصور، الوصف)

---

## 📦 4. اختبار التصدير الجماعي (Bulk Export)

### الخطوات:
1. حدد عدة منتجات للتصدير
2. استخدم API endpoint:
   ```bash
   POST /api/integrations/easy-orders/export-bulk
   {
     "productIds": ["ID1", "ID2", "ID3"],
     "integrationId": "INTEGRATION_ID"
   }
   ```

### النتيجة المتوقعة:
- ✅ رسالة: "تم تصدير X من Y منتج بنجاح"
- ✅ قائمة بالمنتجات الناجحة والفاشلة
- ✅ احترام Rate Limit (1.5 ثانية بين كل طلب)

---

## 🛒 5. اختبار استقبال الطلبات (Webhook)

### إعداد Webhook في EasyOrders:
1. انتقل إلى EasyOrders Dashboard
2. اذهب إلى Public API > Webhooks
3. أضف Webhook URL: `https://your-domain.com/api/integrations/easy-orders/webhook`
4. انسخ Webhook Secret

### تحديث التكامل:
1. في نظام ربح، حدّث التكامل
2. أضف Webhook Secret

### اختبار Webhook:
1. استخدم [webhook.site](https://webhook.site) للاختبار الأولي
2. أو أنشئ طلب تجريبي في EasyOrders
3. تحقق من استقبال الطلب في نظام ربح

### النتيجة المتوقعة:
- ✅ ظهور طلب جديد في `/dashboard/orders`
- ✅ Badge "EasyOrders" على الطلب
- ✅ عرض EasyOrders Order ID في تفاصيل الطلب
- ✅ جميع بيانات الطلب (المنتجات، العميل، العنوان)

---

## 🔍 6. اختبار فلترة الطلبات

### الخطوات:
1. انتقل إلى `/dashboard/orders`
2. في الفلاتر، اختر "مصدر الطلب"
3. اختر "EasyOrders" أو "ربح فقط"

### النتيجة المتوقعة:
- ✅ عرض الطلبات المفلترة فقط
- ✅ Badge "EasyOrders" على الطلبات القادمة من EasyOrders

---

## 🔄 7. اختبار تحديث المنتجات

### الخطوات:
1. قم بتحديث منتج في نظام ربح (السعر، الكمية، إلخ)
2. استخدم API endpoint:
   ```bash
   POST /api/integrations/easy-orders/export-product
   {
     "productId": "PRODUCT_ID",
     "integrationId": "INTEGRATION_ID"
   }
   ```

### النتيجة المتوقعة:
- ✅ تحديث المنتج في EasyOrders
- ✅ رسالة: "تم تحديث المنتج بنجاح"

---

## ⚠️ 8. اختبار معالجة الأخطاء

### اختبار Rate Limit:
1. قم بتصدير أكثر من 40 منتج دفعة واحدة
2. يجب أن يظهر خطأ 429
3. يجب أن يتم Retry تلقائياً مع Exponential Backoff

### اختبار Permission Errors:
1. استخدم API Key بدون صلاحيات كافية
2. يجب أن يظهر خطأ واضح: "ليس لديك صلاحية"

### اختبار Validation Errors:
1. حاول تصدير منتج بدون بيانات مطلوبة
2. يجب أن يظهر خطأ توضيحي

---

## 🧪 9. اختبار Multiple Stores

### الخطوات:
1. أنشئ تكامل EasyOrders أول
2. أنشئ تكامل EasyOrders ثاني (متجر مختلف)
3. تحقق من إمكانية ربط عدة متاجر

### النتيجة المتوقعة:
- ✅ ظهور عدة تكاملات EasyOrders
- ✅ كل متجر له Store ID منفصل
- ✅ Webhook Secret منفصل لكل متجر

---

## 📊 10. اختبار Status Mapping

### التحقق من الحالات:
عند استقبال طلب من EasyOrders، يجب أن يتم تحويل الحالات بشكل صحيح:

| EasyOrders Status | Ribh Status |
|-------------------|-------------|
| pending | pending |
| confirmed | confirmed |
| paid | confirmed |
| processing | processing |
| waiting_for_pickup | ready_for_shipping |
| in_delivery | out_for_delivery |
| delivered | delivered |
| canceled | cancelled |
| refunded | refunded |

### ملاحظة مهمة:
- ❌ **لا يتم تحديث حالة EasyOrders من نظام ربح**
- ✅ يتم فقط استقبال الطلبات وعرضها

---

## 🔐 11. اختبار الأمان

### التحقق من Webhook Secret:
1. أرسل طلب webhook بدون secret header
2. يجب أن يرفض الطلب (401)

### التحقق من API Key:
1. استخدم API Key غير صالح
2. يجب أن يظهر خطأ واضح

---

## 📝 12. اختبار Logging

### التحقق من السجلات:
1. قم بتنفيذ عمليات مختلفة (تصدير، استقبال طلبات)
2. تحقق من السجلات في:
   - Console logs
   - Log files (إذا كان متاحاً)

### النتيجة المتوقعة:
- ✅ تسجيل جميع العمليات
- ✅ تسجيل الأخطاء مع تفاصيل
- ✅ تسجيل Business events

---

## 🎯 13. اختبار Caching

### التحقق من Cache:
1. قم بجلب منتج من EasyOrders
2. قم بجلب نفس المنتج مرة أخرى
3. يجب أن يتم استخدام Cache (لا طلب جديد)

### النتيجة المتوقعة:
- ✅ تقليل عدد الطلبات
- ✅ احترام Rate Limit
- ✅ تحسين الأداء

---

## ✅ قائمة التحقق النهائية

- [ ] الربط التلقائي يعمل
- [ ] مزامنة الشحن تعمل
- [ ] تصدير منتج واحد يعمل
- [ ] التصدير الجماعي يعمل
- [ ] استقبال الطلبات يعمل
- [ ] فلترة الطلبات تعمل
- [ ] تحديث المنتجات يعمل
- [ ] معالجة الأخطاء تعمل
- [ ] Multiple Stores تعمل
- [ ] Status Mapping صحيح
- [ ] الأمان يعمل
- [ ] Logging يعمل
- [ ] Caching يعمل

---

## 🐛 استكشاف الأخطاء

### المشكلة: فشل الربط التلقائي
**الحل:**
- تحقق من Base URL في `.env`
- تحقق من Callback URL
- تحقق من الصلاحيات المطلوبة

### المشكلة: فشل مزامنة الشحن
**الحل:**
- تحقق من وجود مناطق شحن في النظام
- تحقق من API Key permissions
- تحقق من تنسيق البيانات (city:cost)

### المشكلة: فشل استقبال الطلبات
**الحل:**
- تحقق من Webhook Secret
- تحقق من Webhook URL
- استخدم webhook.site للاختبار

### المشكلة: Rate Limit Exceeded
**الحل:**
- انتظر دقيقة واحدة
- قلل عدد المنتجات في Bulk Export
- استخدم Caching

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من السجلات (Logs)
2. تحقق من حالة التكامل
3. راجع رسائل الخطأ
4. تواصل مع فريق الدعم

---

**آخر تحديث:** 2024


