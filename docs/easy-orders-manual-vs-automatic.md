# الربط التلقائي مع EasyOrders

## 🔄 الطريقة الوحيدة للربط

نظام ربح يدعم حالياً **الربط التلقائي فقط** مع EasyOrders. هذه الطريقة هي الأسهل والأكثر أماناً.

### الربط التلقائي (Authorized App Link)

**المميزات:**
- ✅ أسهل - خطوة واحدة فقط
- ✅ يحفظ API Key و Store ID تلقائياً
- ✅ يحفظ Webhook Secret تلقائياً (إذا كان متاحاً)
- ✅ أكثر أماناً - لا حاجة لإدخال API Key يدوياً

**المتطلبات:**
- ⚠️ يحتاج URL عام (ngrok أو Vercel) لأن EasyOrders يرسل callback
- ⚠️ لا يعمل مع localhost مباشرة

---

## 🔧 الإعداد المطلوب

### للربط التلقائي:
1. ✅ URL عام (Vercel أو ngrok)
2. ✅ إضافة Webhook URL في EasyOrders (اختياري - للطلبات)
3. ✅ إضافة Webhook Secret (إذا لم يتم حفظه تلقائياً)

---

## 💡 نصائح للتطوير المحلي

إذا كنت تعمل على localhost:

1. **استخدم ngrok:**
   ```bash
   ngrok http 3000
   ```
   سيحصل على URL عام مثل: `https://abc123.ngrok.io`

2. **استخدم هذا URL في:**
   - متغير البيئة `NEXT_PUBLIC_APP_URL` أو `NEXT_PUBLIC_BASE_URL`
   - EasyOrders callback URL

3. **بعد الربط:**
   - سيتم حفظ API Key و Store ID تلقائياً
   - يمكنك إضافة Webhook Secret يدوياً من EasyOrders Dashboard إذا لزم الأمر

---

## 🎯 للإنتاج

على Vercel أو أي خادم عام:
- ✅ الربط التلقائي يعمل مباشرة
- ✅ لا حاجة لـ ngrok
- ✅ Webhook يعمل تلقائياً

---

**آخر تحديث:** 2024
